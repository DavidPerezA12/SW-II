openapi: 3.0.3
info:
  title: Videogames API
  description: API REST para consultar y gestionar videojuegos, desarrolladores, reseñas y países relacionados.
  version: 1.0.0
servers:
  - url: http://localhost:3001
    description: Servidor local de desarrollo
tags:
  - name: Home
    description: Página inicial de la API
  - name: Games
    description: Operaciones sobre videojuegos
  - name: Developers
    description: Operaciones sobre desarrolladores
  - name: Reviews
    description: Operaciones sobre reseñas
  - name: Countries
    description: Información en XML sobre países y desarrolladores
paths:
  /:
    get:
      tags:
        - Home
      summary: Página de bienvenida
      description: Devuelve la página HTML con enlaces a los endpoints principales.
      responses:
        "200":
          description: Página inicial renderizada correctamente.
          content:
            text/html:
              schema:
                type: string

  /games:
    get:
      tags:
        - Games
      summary: Obtener videojuegos
      description: Lista videojuegos almacenados en MongoDB. Permite filtros, ordenación y paginación.
      parameters:
        - name: id
          in: query
          description: ID numérico del videojuego.
          schema:
            type: integer
          example: 3328
        - name: search
          in: query
          description: Texto a buscar en nombre o slug.
          schema:
            type: string
          example: the witcher
        - name: platform
          in: query
          description: Plataforma del videojuego.
          schema:
            type: string
          example: PC
        - name: genre
          in: query
          description: Género del videojuego.
          schema:
            type: string
          example: Action
        - name: store
          in: query
          description: Tienda en la que está disponible.
          schema:
            type: string
          example: Steam
        - name: minRating
          in: query
          description: Valoración mínima.
          schema:
            type: number
            format: float
          example: 4.5
        - name: page
          in: query
          description: Número de página.
          schema:
            type: integer
            default: 1
          example: 2
        - name: limit
          in: query
          description: Número máximo de resultados.
          schema:
            type: integer
            default: 1000
          example: 20
        - name: sort
          in: query
          description: Campo de ordenación. Con guion inicial ordena de forma descendente.
          schema:
            type: string
          examples:
            ratingAsc:
              value: rating
            nameDesc:
              value: -name
      responses:
        "200":
          description: Lista de videojuegos.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GamesListResponse"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      tags:
        - Games
      summary: Crear videojuego
      description: Inserta un nuevo videojuego en la colección de videojuegos.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GameInput"
      responses:
        "201":
          description: Videojuego creado correctamente.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreatedResponse"
        "400":
          description: Faltan campos obligatorios o hay arrays vacíos.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Ya existe un videojuego con ese ID.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          $ref: "#/components/responses/InternalError"

  /games/{id}:
    get:
      tags:
        - Games
      summary: Obtener videojuego por ID
      parameters:
        - $ref: "#/components/parameters/GameId"
      responses:
        "200":
          description: Videojuego encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Game"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
    put:
      tags:
        - Games
      summary: Actualizar videojuego
      parameters:
        - $ref: "#/components/parameters/GameId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GameUpdate"
      responses:
        "200":
          description: Videojuego actualizado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateGameResponse"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
    delete:
      tags:
        - Games
      summary: Eliminar videojuego
      parameters:
        - $ref: "#/components/parameters/GameId"
      responses:
        "200":
          description: Videojuego eliminado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"

  /games/{id}/country:
    get:
      tags:
        - Games
        - Countries
      summary: Obtener países relacionados con un videojuego
      description: Devuelve información de países en formato XML.
      parameters:
        - $ref: "#/components/parameters/GameId"
      responses:
        "200":
          description: Información de países en XML.
          content:
            application/xml:
              schema:
                $ref: "#/components/schemas/CountriesXml"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"

  /games/{id}/reviews:
    get:
      tags:
        - Games
        - Reviews
      summary: Obtener reseñas de un videojuego
      parameters:
        - $ref: "#/components/parameters/GameId"
      responses:
        "200":
          description: Reseñas asociadas al videojuego.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GameReviewsResponse"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"

  /games/{id}/enriched:
    get:
      tags:
        - Games
      summary: Obtener videojuego enriquecido
      description: Devuelve el videojuego junto con información de países y reseñas.
      parameters:
        - $ref: "#/components/parameters/GameId"
      responses:
        "200":
          description: Videojuego con datos relacionados.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnrichedGameResponse"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"

  /developers:
    get:
      tags:
        - Developers
      summary: Obtener desarrolladores
      description: Lista desarrolladores almacenados en MongoDB. Permite búsqueda, filtro por videojuego, ordenación y paginación.
      parameters:
        - name: search
          in: query
          description: Texto a buscar en nombre o slug.
          schema:
            type: string
          example: Valve
        - name: gameId
          in: query
          description: ID de un videojuego asociado al desarrollador.
          schema:
            type: integer
          example: 3328
        - name: page
          in: query
          description: Número de página.
          schema:
            type: integer
            default: 1
          example: 2
        - name: limit
          in: query
          description: Número máximo de resultados.
          schema:
            type: integer
            default: 1000
          example: 20
        - name: sort
          in: query
          description: Campo de ordenación. Con guion inicial ordena de forma descendente.
          schema:
            type: string
          example: -games_count
      responses:
        "200":
          description: Lista de desarrolladores.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DevelopersListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      tags:
        - Developers
      summary: Crear desarrollador
      description: Inserta un nuevo desarrollador en la colección de desarrolladores.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DeveloperInput"
      responses:
        "201":
          description: Desarrollador creado correctamente.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreatedDeveloperResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "409":
          description: Ya existe un desarrollador con ese ID.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          $ref: "#/components/responses/InternalError"

  /developers/{id}:
    get:
      tags:
        - Developers
      summary: Obtener desarrollador por ID
      parameters:
        - name: id
          in: path
          required: true
          description: ID numérico del desarrollador.
          schema:
            type: integer
          example: 1612
      responses:
        "200":
          description: Desarrollador encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Developer"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
    put:
      tags:
        - Developers
      summary: Actualizar desarrollador
      parameters:
        - name: id
          in: path
          required: true
          description: ID numérico del desarrollador.
          schema:
            type: integer
          example: 1612
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DeveloperUpdate"
      responses:
        "200":
          description: Desarrollador actualizado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdatedDeveloperResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
    delete:
      tags:
        - Developers
      summary: Eliminar desarrollador
      parameters:
        - name: id
          in: path
          required: true
          description: ID numérico del desarrollador.
          schema:
            type: integer
          example: 1612
      responses:
        "200":
          description: Desarrollador eliminado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"

  /reviews:
    get:
      tags:
        - Reviews
      summary: Obtener reseñas
      description: Lista reseñas almacenadas en MongoDB. Permite filtrar por ID y rating.
      parameters:
        - name: id
          in: query
          description: ID numérico de la reseña.
          schema:
            type: integer
          example: 4
        - name: rating
          in: query
          description: Valoración de la reseña.
          schema:
            type: integer
            minimum: 1
            maximum: 5
          example: 5
        - name: page
          in: query
          description: Número de página.
          schema:
            type: integer
            default: 1
          example: 2
        - name: limit
          in: query
          description: Número máximo de resultados.
          schema:
            type: integer
            default: 1000
          example: 20
      responses:
        "200":
          description: Lista de reseñas.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReviewsListResponse"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      tags:
        - Reviews
      summary: Crear reseña
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReviewInput"
      responses:
        "201":
          description: Reseña creada correctamente.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreatedReviewResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "409":
          description: Ya existe una reseña con ese ID.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          $ref: "#/components/responses/InternalError"

  /reviews/{id}:
    get:
      tags:
        - Reviews
      summary: Obtener reseñas por videojuego
      parameters:
        - name: id
          in: path
          required: true
          description: ID numérico del videojuego.
          schema:
            type: integer
          example: 3328
      responses:
        "200":
          description: Reseñas del videojuego.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReviewsByGameResponse"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"

    patch:
      tags:
        - Reviews
      summary: Actualizar reseña
      parameters:
        - name: id
          in: path
          required: true
          description: ID numérico de la reseña.
          schema:
            type: integer
          example: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReviewUpdate"
      responses:
        "200":
          description: Reseña actualizada.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdatedReviewResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Ya existe una reseña con ese ID.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          $ref: "#/components/responses/InternalError"
    delete:
      tags:
        - Reviews
      summary: Eliminar reseña
      parameters:
        - name: id
          in: path
          required: true
          description: ID numérico de la reseña.
          schema:
            type: integer
          example: 1
      responses:
        "200":
          description: Reseña eliminada.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"

  /countries:
    get:
      tags:
        - Countries
      summary: Obtener países
      description: Devuelve información de países en formato XML.
      parameters:
        - name: country
          in: query
          description: Nombre del país.
          schema:
            type: string
          example: Poland
        - name: developer
          in: query
          description: Nombre del desarrollador.
          schema:
            type: string
          example: CD Projekt RED
        - name: gameName
          in: query
          description: Nombre del videojuego.
          schema:
            type: string
          example: "The Witcher 3: Wild Hunt"
      responses:
        "200":
          description: Información de países en XML.
          content:
            application/xml:
              schema:
                $ref: "#/components/schemas/CountriesXml"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"

components:
  parameters:
    GameId:
      name: id
      in: path
      required: true
      description: ID numérico del videojuego.
      schema:
        type: integer
      example: 3328

  responses:
    BadRequest:
      description: Petición incorrecta.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    NotFound:
      description: Recurso no encontrado.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    InternalError:
      description: Error interno del servidor.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"

  schemas:
    Game:
      type: object
      properties:
        _id:
          type: string
          description: Identificador interno de MongoDB.
        id:
          type: integer
          example: 3328
        slug:
          type: string
          example: the-witcher-3-wild-hunt
        name:
          type: string
          example: "The Witcher 3: Wild Hunt"
        released:
          type: string
          nullable: true
          example: "2015-05-18"
        background_image:
          type: string
          nullable: true
          example: https://media.rawg.io/media/games/618/618c2031a07bbff6b4f611f10b6bcdbc.jpg
        rating:
          type: number
          format: float
          nullable: true
          example: 4.64
        metacritic:
          type: integer
          nullable: true
          example: 92
        playtime:
          type: integer
          nullable: true
          example: 43
        platforms:
          type: array
          items:
            type: string
          example:
            - PC
            - PlayStation 4
        genres:
          type: array
          items:
            type: string
          example:
            - Action
            - RPG
        stores:
          type: array
          items:
            type: string
          example:
            - Steam
            - GOG
        esrb_rating:
          type: string
          nullable: true
          example: Mature

    GameInput:
      type: object
      required:
        - id
        - name
        - platforms
        - genres
        - stores
      properties:
        id:
          type: integer
          example: 999999
        slug:
          type: string
          example: example-game
        name:
          type: string
          example: Example Game
        released:
          type: string
          example: "2026-01-01"
        background_image:
          type: string
          nullable: true
        rating:
          type: number
          format: float
        metacritic:
          type: integer
          nullable: true
        playtime:
          type: integer
        platforms:
          type: array
          minItems: 1
          items:
            type: string
        genres:
          type: array
          minItems: 1
          items:
            type: string
        stores:
          type: array
          minItems: 1
          items:
            type: string
        esrb_rating:
          type: string
          nullable: true

    GameUpdate:
      type: object
      description: Campos del videojuego que se quieran modificar.
      properties:
        slug:
          type: string
        name:
          type: string
        released:
          type: string
        background_image:
          type: string
          nullable: true
        rating:
          type: number
          format: float
        metacritic:
          type: integer
          nullable: true
        playtime:
          type: integer
        platforms:
          type: array
          items:
            type: string
        genres:
          type: array
          items:
            type: string
        stores:
          type: array
          items:
            type: string
        esrb_rating:
          type: string
          nullable: true

    DeveloperGame:
      type: object
      properties:
        id:
          type: integer
          example: 4200
        slug:
          type: string
          example: portal-2
        name:
          type: string
          example: Portal 2
        added:
          type: integer
          example: 20858

    Developer:
      type: object
      properties:
        _id:
          type: string
          description: Identificador interno de MongoDB.
        id:
          type: integer
          example: 1612
        name:
          type: string
          example: Valve Software
        slug:
          type: string
          example: valve-software
        games_count:
          type: integer
          example: 44
        image_background:
          type: string
          nullable: true
        games:
          type: array
          items:
            $ref: "#/components/schemas/DeveloperGame"

    DeveloperInput:
      type: object
      required:
        - id
        - name
        - slug
        - games
      properties:
        id:
          type: integer
          example: 999999
        name:
          type: string
          example: Mi Estudio
        slug:
          type: string
          example: mi-estudio
        games_count:
          type: integer
          example: 1
        image_background:
          type: string
          nullable: true
          example: https://example.com/developer.jpg
        games:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/DeveloperGame"

    DeveloperUpdate:
      type: object
      description: Campos del desarrollador que se quieran modificar. El ID no se puede actualizar.
      properties:
        name:
          type: string
        slug:
          type: string
        games_count:
          type: integer
        image_background:
          type: string
          nullable: true
        games:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/DeveloperGame"

    Review:
      type: object
      properties:
        _id:
          type: string
          description: Identificador interno de MongoDB.
        id:
          type: integer
          example: 1
        gameId:
          type: integer
          example: 3328
        gameName:
          type: string
          example: "The Witcher 3: Wild Hunt"
        user:
          type: string
          example: player_01
        rating:
          type: integer
          minimum: 1
          maximum: 5
          example: 5
        comment:
          type: string
          example: Muy buen juego, lo recomiendo.
        createdAt:
          type: string
          format: date-time

    ReviewInput:
      type: object
      required:
        - id
        - gameId
        - gameName
        - rating
        - comment
      properties:
        id:
          type: integer
          example: 4000
        gameId:
          type: integer
          example: 3328
        gameName:
          type: string
          example: "The Witcher 3: Wild Hunt"
        user:
          type: string
          example: player_01
        rating:
          type: integer
          minimum: 1
          maximum: 5
          example: 5
        comment:
          type: string
          example: Muy buen juego, lo recomiendo.

    ReviewUpdate:
      type: object
      properties:
        id:
          type: integer
        gameId:
          type: integer
        gameName:
          type: string
        user:
          type: string
        rating:
          type: integer
          minimum: 1
          maximum: 5
        comment:
          type: string

    Country:
      type: object
      properties:
        gameId:
          type: integer
          example: 3328
        gameName:
          type: string
          example: "The Witcher 3: Wild Hunt"
        developer:
          type: string
          example: CD Projekt RED
        country:
          type: string
          example: Poland

    CountriesXml:
      type: object
      xml:
        name: countries
      properties:
        country:
          type: array
          xml:
            wrapped: false
            name: country
          items:
            $ref: "#/components/schemas/Country"

    GamesListResponse:
      type: object
      properties:
        videogames_length:
          type: integer
          example: 2
        videogames:
          type: array
          items:
            $ref: "#/components/schemas/Game"

    DevelopersListResponse:
      type: object
      properties:
        developers_length:
          type: integer
          example: 2
        developers:
          type: array
          items:
            $ref: "#/components/schemas/Developer"

    ReviewsListResponse:
      type: object
      properties:
        total:
          type: integer
          example: 3437
        reviews_length:
          type: integer
          example: 20
        reviews:
          type: array
          items:
            $ref: "#/components/schemas/Review"

    ReviewsByGameResponse:
      type: object
      properties:
        reviews_length:
          type: integer
          example: 3
        reviews:
          type: array
          items:
            $ref: "#/components/schemas/Review"

    GameReviewsResponse:
      type: object
      properties:
        gameId:
          type: integer
          example: 3328
        gameName:
          type: string
          example: "The Witcher 3: Wild Hunt"
        reviews_length:
          type: integer
          example: 3
        reviews:
          type: array
          items:
            $ref: "#/components/schemas/Review"

    EnrichedGameResponse:
      type: object
      properties:
        game:
          $ref: "#/components/schemas/Game"
        wikidata:
          type: object
          properties:
            countries:
              type: array
              items:
                $ref: "#/components/schemas/Country"
        reviews:
          type: array
          items:
            $ref: "#/components/schemas/Review"

    CreatedResponse:
      type: object
      properties:
        message:
          type: string
          example: Game created successfully
        insertedId:
          type: string

    CreatedReviewResponse:
      type: object
      properties:
        message:
          type: string
          example: Review created successfully
        insertedId:
          type: string
        review:
          $ref: "#/components/schemas/Review"

    CreatedDeveloperResponse:
      type: object
      properties:
        message:
          type: string
          example: Desarrollador creado correctamente
        insertedId:
          type: string
        developer:
          $ref: "#/components/schemas/Developer"

    UpdatedDeveloperResponse:
      type: object
      properties:
        message:
          type: string
          example: Desarrollador actualizado correctamente
        developer:
          $ref: "#/components/schemas/Developer"

    UpdatedReviewResponse:
      type: object
      properties:
        message:
          type: string
          example: Review updated successfully
        review:
          $ref: "#/components/schemas/Review"

    UpdateGameResponse:
      type: object
      properties:
        message:
          type: string
          example: Game updated successfully
        id:
          type: integer
          example: 3328

    MessageResponse:
      type: object
      properties:
        message:
          type: string

    ErrorResponse:
      type: object
      properties:
        message:
          type: string
        error:
          nullable: true
